# 2026-03-13 - SDK 对齐 Core v0.4.0

## 背景 / 目标
- `MyFlowHub-Core` 已发布 `v0.4.0`。
- 本次将 `MyFlowHub-SDK` 的 `Core` 依赖对齐到发布版本，保证 SDK 可直接使用最新内核能力而不依赖本地 workspace。

## 具体变更
- `go.mod`
  - `github.com/yttydcs/myflowhub-core: v0.3.1 -> v0.4.0`
- `go.sum`
  - 同步 `Core v0.4.0` 校验信息

## 设计决策与权衡
- 仅做版本对齐，不修改 SDK 的 session / transport / await 逻辑。
- 使用 `GOWORK=off` 做模块级验证，确保真实发布依赖图可用。
- 若升级暴露兼容问题，再进入最小修复；本次未触发。

## 验证
- 目录：`worktrees/chore-bump-core-v0.4.0-downstream/repo/MyFlowHub-SDK`
- 命令：`$env:GOWORK='off'; go test ./... -count=1`
- 结果：通过

## 风险与回滚
- 风险较低：仅升级依赖版本。
- 如需回滚，可回退本次提交并恢复 `myflowhub-core v0.3.1`。
