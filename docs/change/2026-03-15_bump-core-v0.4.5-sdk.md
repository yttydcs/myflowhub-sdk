# 2026-03-15 - SDK：对齐 Core 到 v0.4.5

## 变更背景 / 目标
- 背景：Core 发布 `v0.4.5`，修复 Windows RFCOMM 流式读写兼容问题。
- 目标：SDK 依赖对齐到 `myflowhub-core v0.4.5`，确保 Win 下游能直接获得修复链路。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.4` -> `v0.4.5`

## 任务映射
- `SDK-1` 依赖升级
- `SDK-2` 测试验证
- `SDK-3` 文档归档
- `SDK-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅依赖版本升级，不改 SDK 业务逻辑。
- 回滚：回退 `go.mod` 与本归档文档。
