# SDK：升级 myflowhub-core 到 v0.3.1（RFCOMM 发布对齐）

## 背景 / 目标
- Core 已发布 `v0.3.1`，SDK 主分支也已接入 RFCOMM endpoint dial。
- 当前 SDK `go.mod` 仍引用 `myflowhub-core v0.2.0`，与主分支代码不匹配。
- 目标：将 SDK 依赖对齐到 `v0.3.1`，保证 `GOWORK=off` 下可拉取、可测试、可发布。

## 具体变更
- `go.mod`：`github.com/yttydcs/myflowhub-core v0.2.0 -> v0.3.1`
- `go.sum`：更新校验和

## 设计与权衡
- 仅做依赖对齐，不改 Session / transport 行为。
- 发布 patch 版本 `v0.1.3`，保持现有 SDK 版本节奏。

## 测试与验证
- `cd repo/MyFlowHub-SDK; $env:GOWORK='off'; go test ./... -count=1`

## 回滚
- revert 本次依赖升级提交；如已发布，则通过更高 patch tag 修正。
