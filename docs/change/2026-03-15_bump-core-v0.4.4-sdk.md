# 2026-03-15 - SDK：对齐 Core 到 v0.4.4

## 变更背景 / 目标
- 背景：Core 发布 `v0.4.4`，包含 Windows RFCOMM 连接中止相关 transport 修复。
- 目标：将 SDK 依赖对齐到 `myflowhub-core v0.4.4`，为 Win/其它下游提供统一修复基线。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.3` -> `v0.4.4`

## 任务映射
- `SDK-1` 依赖升级
- `SDK-2` 测试验证
- `SDK-3` 文档归档
- `SDK-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅依赖版本提升，不引入业务行为变更。
- 回滚：回退 `go.mod` 与本归档文档。
